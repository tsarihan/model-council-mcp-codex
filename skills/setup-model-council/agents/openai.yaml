interface:
  display_name: "Set Up Model Council"
  short_description: "Configure council tiers, members, and defaults"
  default_prompt: "Use $setup-model-council to configure my model council."
dependencies:
  tools:
    - type: "mcp"
      value: "model-council"
      description: "Model Council setup and configuration tools"
policy:
  allow_implicit_invocation: true
