{
  "name": "model-council",
  "description": "Ask local and cloud AI models for individual, categorized, deconflicted, pooled, or dialectic answers.",
  "version": "0.2.86",
  "author": {
    "name": "Tom Sarihan"
  },
  "homepage": "https://github.com/tsarihan/model-council-mcp-codex",
  "repository": "https://github.com/tsarihan/model-council-mcp-codex",
  "license": "Apache-2.0",
  "keywords": [
    "ollama",
    "llm",
    "council",
    "multi-model",
    "vllm",
    "deconflict"
  ],
  "userConfig": {
    "ollama_address": {
      "type": "string",
      "title": "Ollama address",
      "description": "Base URL of your Ollama server. Override host/port if Ollama runs remotely or on a non-default port.",
      "default": "http://localhost:11434"
    },
    "council_models": {
      "type": "string",
      "title": "Council models (optional)",
      "description": "Leave BLANK for zero-config: the council auto-uses every local Ollama model plus your Ollama :cloud models. Or pin specific ones, comma-separated: 'ollama:llama3,ollama:qwen3.6:27b-mlx,openai:gpt-4o'. Format: provider:model or provider/serverId:model.",
      "default": ""
    },
    "auto_council": {
      "type": "boolean",
      "title": "Auto-discover council",
      "description": "When ON (default) and no council models are pinned above, automatically use all available Ollama chat models (local + :cloud), excluding embedding models.",
      "default": true
    },
    "claude_tier": {
      "type": "string",
      "title": "Claude subscription tier",
      "description": "Your Claude plan: free | pro | max5x | max20x. Sets Claude concurrency and whether subscription-backed Claude members are added (only if the claude CLI is installed AND logged in). free = no Claude members. Tip: run /model-council:setup to choose interactively.",
      "default": "pro"
    },
    "chatgpt_tier": {
      "type": "string",
      "title": "ChatGPT subscription tier",
      "description": "Your ChatGPT plan: free | plus | pro5x | pro20x. Adds ChatGPT/Codex members (only if the codex CLI is installed AND signed in). free = no ChatGPT members.",
      "default": "plus"
    },
    "ollama_tier": {
      "type": "string",
      "title": "Ollama subscription tier",
      "description": "Your Ollama plan: free | pro | max. free = local models only (no cloud). pro = cloud on, 3 concurrent; max = cloud on, 10 concurrent.",
      "default": "pro"
    },
    "grok_tier": {
      "type": "string",
      "title": "Grok subscription tier",
      "description": "Your X.AI Grok plan: free | supergrok | premiumplus | heavy. Adds Grok members via the local `grok` CLI (only if it is installed AND logged in). Defaults to free (opt-in) since this is a newer provider added on top of an existing install base — set this or run /model-council:setup to add Grok members.",
      "default": "free"
    },
    "judge_model": {
      "type": "string",
      "title": "Judge model",
      "description": "Model that categorizes and deconflicts council responses. Use 'auto' to pick the largest council member.",
      "default": "auto"
    },
    "response_mode": {
      "type": "string",
      "title": "Default response mode",
      "description": "One of: individual | categorized | deconflicted | pooled | dialectic. individual = raw answers; categorized = grouped into agreement/complementary/conflicting; deconflicted = iterative resolution loop with a score; pooled = Delphi-style neutral reconsideration (no attribution/ranking shown to members, divergence preserved); dialectic = thesis/antithesis/synthesis (defend your pick, judge builds pros/cons, members re-select).",
      "default": "categorized"
    },
    "max_deconflict_rounds": {
      "type": "number",
      "title": "Max deconfliction rounds",
      "description": "Maximum rounds the judge re-questions the council to resolve conflicts (1-10).",
      "default": 3,
      "min": 1,
      "max": 10
    },
    "openai_api_key": {
      "type": "string",
      "title": "OpenAI API key",
      "description": "Optional. Enables OpenAI models (gpt-4o, o1, ...) in the council.",
      "sensitive": true
    },
    "anthropic_api_key": {
      "type": "string",
      "title": "Anthropic API key",
      "description": "Optional. Enables Anthropic Claude models in the council.",
      "sensitive": true
    },
    "xai_api_key": {
      "type": "string",
      "title": "X.AI (Grok) API key",
      "description": "Optional. Enables X.AI's Grok models in the council.",
      "sensitive": true
    },
    "vllm_servers": {
      "type": "string",
      "title": "vLLM servers",
      "description": "Optional. Comma-separated 'name:host:port' entries for OpenAI-compatible vLLM servers. Port defaults to 8000. Example: 'gpu1:192.168.1.10:8000,gpu2:192.168.1.10:8001'.",
      "default": ""
    },
    "trtllm_servers": {
      "type": "string",
      "title": "TensorRT-LLM servers",
      "description": "Optional. Comma-separated 'name:host:port' entries for OpenAI-compatible TRT-LLM servers. Port defaults to 8000.",
      "default": ""
    },
    "sglang_servers": {
      "type": "string",
      "title": "SGLang servers",
      "description": "Optional. Comma-separated 'name:host:port' entries for OpenAI-compatible SGLang servers. Port defaults to 30000.",
      "default": ""
    },
    "max_tokens": {
      "type": "number",
      "title": "Max response tokens",
      "description": "Maximum OUTPUT tokens requested per completion. Clamped per-model to fit each server's context window, so a generous value gives longer answers on large-context models. CLI members (claude/codex/grok) ignore it (subscription-managed).",
      "default": 32768,
      "min": 256,
      "max": 200000
    },
    "reasoning_effort": {
      "type": "string",
      "title": "Default reasoning effort",
      "description": "How hard every council member AND the judge think: none | minimal | low | medium | high | xhigh | max. A brand-new install starts at high (seeded once, then yours to change); leave empty to use each model's own default. Higher levels give deeper answers at real cost in time and subscription quota, multiplied across members x rounds. A level a given backend doesn't support is clamped to its nearest supported one (e.g. 'max' runs as 'high' on an Ollama model, 'none' as 'low' on the Claude CLI), so one setting works across a mixed council. Override per call with ask_council's reasoning_effort."
    },
    "web_access": {
      "type": "boolean",
      "title": "Web access (research mode)",
      "description": "When ON, council members search the web instead of answering from training data. Subscription-CLI members search directly, and a bare Ollama member is automatically re-pointed through the claude-CLI harness so it can research too; API-key providers have no tool loop and still answer from memory (the result says who did which). WARNING: pulls untrusted page content into member answers, and costs real latency and subscription quota. Override per call with ask_council's web_access.",
      "default": false
    },
    "cloud_concurrency": {
      "type": "number",
      "title": "Cloud concurrency (override)",
      "description": "Optional. Caps ALL cloud pools at this value, overriding the per-tier limits. Leave unset to let your subscription tiers set per-provider concurrency (e.g. ChatGPT 6, Claude 2, Ollama cloud 3/10).",
      "min": 1,
      "max": 100
    },
    "local_concurrency": {
      "type": "number",
      "title": "Local concurrency",
      "description": "Max simultaneous requests to local models. Default 1 (sequential) to avoid resource contention; set 0 for unlimited.",
      "default": 1,
      "min": 0,
      "max": 100
    },
    "completion_retries": {
      "type": "number",
      "title": "Completion retries",
      "description": "Attempts per completion before giving up on an empty or failed response.",
      "default": 3,
      "min": 1,
      "max": 10
    },
    "request_timeout_ms": {
      "type": "number",
      "title": "Per-request timeout — text-only (ms)",
      "description": "Wall-clock timeout for a single model completion on text-only calls (no full_repo_access) before it's recorded as a timed-out member (default 300000 = 5 min; local Ollama models run sequentially, so a busy box needs headroom). Raise it for slow local models (large MLX/GGUF) — it's honoured verbatim by every provider, including claude-cli/codex-cli/grok-cli (no 300s floor). Change at runtime with the set_council_timeouts MCP tool.",
      "default": 300000,
      "min": 5000,
      "max": 1800000
    },
    "repo_request_timeout_ms": {
      "type": "number",
      "title": "Per-request timeout — full_repo_access (ms)",
      "description": "Per-completion timeout used when full_repo_access is set (default 600000 = 10 min) — the CLI member Read/Grep/Globs the repo tree, materially longer than a flat text completion. Honoured verbatim by every provider. Change at runtime with the set_council_timeouts MCP tool.",
      "default": 600000,
      "min": 5000,
      "max": 1800000
    },
    "deconflict_verbose": {
      "type": "boolean",
      "title": "Verbose deconfliction",
      "description": "When on, deconflicted results include the initial categorization and per-round detail by default.",
      "default": false
    },
    "claude_cli": {
      "type": "boolean",
      "title": "Claude subscription (CLI)",
      "description": "Add subscription-backed Claude members via the local `claude` CLI (claude -p) instead of an API key. Requires the Claude Code CLI installed and logged in to a Pro/Max subscription. Members appear as claude-cli:<model>. Only works where the CLI runs (this machine).",
      "default": false
    },
    "claude_cli_models": {
      "type": "string",
      "title": "Claude CLI models",
      "description": "Comma-separated model aliases for the subscription CLI member, e.g. 'opus,sonnet'.",
      "default": "opus,sonnet"
    },
    "claude_cli_path": {
      "type": "string",
      "title": "claude executable path",
      "description": "Path to the claude binary if it is not on PATH.",
      "default": "claude"
    },
    "codex_cli": {
      "type": "boolean",
      "title": "ChatGPT subscription (Codex CLI)",
      "description": "Add a ChatGPT-subscription member via the local `codex exec` CLI instead of an OpenAI API key. Requires the Codex CLI installed and signed in (`codex login` → Sign in with ChatGPT). Codex is a coding agent, so answers are coding-flavored. Members appear as codex-cli:<model>.",
      "default": false
    },
    "codex_cli_models": {
      "type": "string",
      "title": "Codex models",
      "description": "Comma-separated model names for the Codex member. Use 'default' for Codex's configured default model.",
      "default": "default"
    },
    "codex_cli_path": {
      "type": "string",
      "title": "codex executable path",
      "description": "Path to the codex binary if it is not on PATH.",
      "default": "codex"
    },
    "grok_cli": {
      "type": "boolean",
      "title": "Grok subscription (CLI)",
      "description": "Add subscription-backed Grok members via the local `grok` CLI instead of an X.AI API key. Requires xAI's Grok Build CLI installed and logged in to a SuperGrok/X Premium+/Heavy subscription. Members appear as grok-cli:<model>. Only works where the CLI runs (this machine).",
      "default": false
    },
    "grok_cli_models": {
      "type": "string",
      "title": "Grok CLI models",
      "description": "Comma-separated model names for the subscription CLI member, e.g. 'grok-4.5'.",
      "default": "grok-4.5"
    },
    "grok_cli_path": {
      "type": "string",
      "title": "grok executable path",
      "description": "Path to the grok binary if it is not on PATH.",
      "default": "grok"
    }
  }
}
